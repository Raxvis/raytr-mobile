import { Text, View } from 'react-native';

const LoadingScreen = () => (
  <View className="flex h-full w-full flex-row items-center justify-center bg-black">
    <Text className="font-poppins text-2xl">RAYTR</Text>
  </View>
);

export default LoadingScreen;
